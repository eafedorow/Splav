<UserControl x:Class="Splav2.Views.UserControlOpenDB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Splav2.ViewModels"
             xmlns:local="clr-namespace:Splav2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModel:ViewModelOpenDB}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="File" Command="{Binding OpenExcel}" Margin="5"/>
        <TextBlock Text="{Binding FilePath}" Grid.Row="1" Margin="5"/>
        <TextBlock Text="{Binding RowCount, StringFormat='Rows: {0}'}" Grid.Row="2" Margin="5"/>
        <TextBlock Text="{Binding ColumnCount, StringFormat='Columns: {0}'}" Grid.Row="3" Margin="5"/>
        <DataGrid ItemsSource="{Binding ReadExcelData}" Grid.Row="4" Margin="5"/>
    </Grid>
</UserControl>
